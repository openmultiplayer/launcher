; ============================================================================
;  TrackpadFixSA - DIAGNOSTIC config (isolation test)
;  Enabled=0 -> proxy loads and forwards real dinput8 but installs NO hooks
;  and NO vtable patches. Used to prove whether the crash is the hooks or the
;  copied builtin dinput8. Full tuning config restored after this test.
; ============================================================================
[TrackpadFix]
Enabled = 0
Logging = 1
